@{
    ViewData["Title"] = "Home";
}

<style>
    .hero {
        background: radial-gradient(circle at top left, #fff3da, #ffd48f);
        border-radius: 26px;
        padding: 4rem 3rem;
        overflow: hidden;
        position: relative;
    }

    .hero:after {
        content: "";
        position: absolute;
        right: -120px;
        top: -120px;
        width: 320px;
        height: 320px;
        background: rgba(255,255,255,0.45);
        border-radius: 50%;
        filter: blur(2px);
    }

    .hero h1 {
        font-weight: 800;
        letter-spacing: -0.02em;
    }

    .hero p {
        font-size: 1.1rem;
        color: #5c5c5c;
        max-width: 720px;
        margin: 0 auto;
    }

    .pill {
        display: inline-flex;
        align-items: center;
        gap: .5rem;
        padding: .45rem .8rem;
        border-radius: 999px;
        background: rgba(255,255,255,0.75);
        border: 1px solid rgba(0,0,0,0.06);
        font-size: .9rem;
        color: #5c5c5c;
    }

    .feature-card {
        border: none;
        border-radius: 22px;
        transition: transform .2s ease, box-shadow .2s ease;
        background: #fff;
    }

    .feature-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 12px 30px rgba(0,0,0,.12);
    }

    .feature-icon {
        width: 46px;
        height: 46px;
        border-radius: 14px;
        display: grid;
        place-items: center;
        background: rgba(255,212,143,0.45);
        font-size: 1.35rem;
    }

    .soft {
        color: #6c757d;
    }

    .cta {
        border-radius: 22px;
        background: #fff;
        border: 1px solid rgba(0,0,0,0.08);
        padding: 1.2rem 1.4rem;
    }
</style>

<div class="container">

    <!-- HERO -->
    <div class="hero text-center mb-5">
        <div class="d-flex justify-content-center gap-2 flex-wrap mb-3">
            <span class="pill">🇩🇪 Немски • A1–B2</span>
            <span class="pill">📚 Курсове + уроци</span>
            <span class="pill">✨ Учи по твоето темпо</span>
        </div>

        <h1 class="mb-3">Започни да учиш немски по-лесно</h1>
        <p class="mb-4">
            Избери курс по ниво, следвай уроците и учи стъпка по стъпка.
            Всичко е подредено ясно, за да напредваш спокойно.
        </p>

        <div class="d-flex justify-content-center gap-3 flex-wrap">
            <a asp-controller="Course" asp-action="Index"
               class="btn btn-primary btn-lg px-4">
                📚 Разгледай курсове
            </a>

            <a asp-controller="Lesson" asp-action="Index"
               class="btn btn-outline-secondary btn-lg px-4">
                ▶️ Започни с уроците
            </a>
        </div>
    </div>

    <!-- FEATURES -->
    <div class="row g-4 mb-5">
        <div class="col-md-4">
            <div class="card feature-card h-100 p-4">
                <div class="d-flex align-items-center gap-3 mb-2">
                    <div class="feature-icon">🧩</div>
                    <h5 class="mb-0">Уроци на малки стъпки</h5>
                </div>
                <p class="soft mb-0">
                    Кратки, ясни уроци с добре подредено съдържание, за да учиш без стрес.
                </p>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card feature-card h-100 p-4">
                <div class="d-flex align-items-center gap-3 mb-2">
                    <div class="feature-icon">🎯</div>
                    <h5 class="mb-0">Нива A1–B2</h5>
                </div>
                <p class="soft mb-0">
                    Подреди обучението си по ниво и следвай курса, който ти е подходящ.
                </p>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card feature-card h-100 p-4">
                <div class="d-flex align-items-center gap-3 mb-2">
                    <div class="feature-icon">⏱️</div>
                    <h5 class="mb-0">Учи по твоето темпо</h5>
                </div>
                <p class="soft mb-0">
                    Върви напред когато имаш време — уроците са винаги под ръка.
                </p>
            </div>
        </div>
    </div>

    <!-- CTA -->
    <div class="cta d-flex flex-column flex-md-row justify-content-between align-items-center gap-3 mb-4">
        <div>
            <h5 class="mb-1">Готова ли си да започнеш?</h5>
            <div class="soft">Избери курс и започни още днес.</div>
        </div>

        <div class="d-flex gap-2">
            <a asp-controller="Course" asp-action="Index" class="btn btn-primary px-4">
                Избери курс
            </a>
            <a asp-controller="Home" asp-action="About" class="btn btn-outline-secondary px-4">
                Как работи
            </a>
        </div>
    </div>

    <div class="text-center text-muted small mb-4">
        © @DateTime.Now.Year • LernEasy Deutsch
    </div>
</div>
